<% if @restaurant_menu.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@restaurant_menu.errors.count, "error") %> prohibited this menu from being saved:</h2>

    <ul>
      <% @restaurant_menu.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

  <%= form_with url: restaurant_restaurant_menus_new_path do |form| %>
    <div class="col-sm-10">
      <label class="form-label">Menu File</label>
      <%= form.file_field :menu_file, class: "form-control" %>
    </div>
    <br>
    <%= form.submit 'Upload', class: "btn btn-danger" %>
  <% end %>
